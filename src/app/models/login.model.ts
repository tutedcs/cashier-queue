export interface userLogin {
    usuario: string;
    password: string;
}

export interface userRegister {
    nombre: string;
    apellido: string;
    usuario: string;
    password: string;
    rol: number;
}